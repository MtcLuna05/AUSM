# AUSM runtime handoff

- 2026-09-05 Java 8 port: cherry-picked all four fixes commits through master81ac3cb and the 1.0.4 changelog draft. Preserved Forge/Java8 configuration; restored vertexBufferFormat reflection helper required by new LittleTiles code. Built build/libs/AUSM-1.0.4-Java8.jar, SHA-256 edc1ef1f2e428a5e076669bb0308e0c2bdf876fe53c698ffcb08d7027f4b1c39. Archive integrity passes; all609 classes have major version<=52. Full suite:97 passed,2 skipped,2 source-convention failures (PipelineFrameLayerCapture1355 lines; inline qualified type in PipelineRuntimeSkyDetailState), identical affected source files on master. No Java8 runtime/install validation. No push or game restart.

- Java 8 port targets 1.0.4. Runtime acceptance below refers to the Java 25 MBC client, not Java 8 runtime validation. Preserve Forge/Java 8 build configuration; do not install or restart a client without asking.
- 2026-09-05 user visual acceptance: the live-program-interface fix resolved the shaderless bloom leak ("this fixed it"). Hand leak was also previously accepted. User requested committing all changes locally, no push. Latest build/install SHA-256: 1f48d58f9e1c814c7e679e48c9720ee90165991584f481a15ae54a538243c292. Ten routing regression tests pass; MBC remains running with helper hot swaps applied. Generated logs and stray --help PostScript artifact are excluded from the source commit and left untouched.

- Live-interface fix persisted: installMbcJar succeeded after the13:20:16 hot swap. Ten tests pass, game running, no active agent probes. Await user visual on->off validation; no restart/commit/push.

- Live-interface fix validation: ten routing tests passed; NothiriumShadowCompileScheduling hot-swapped. Postpatch breakpoint at drawChunks:316 now hits native Bloom scalar GL_QUADS draw (before fix this path was bypassed). Program115, stride56, chunk(224,64,272), camera(231.8558701,64,279.3993411), GPU ausm_ChunkOffset=[-7.8558702,0,-7.399341] matches expected. User moved scenes during build, so no same-scene after screenshot or visual acceptance yet. Probes removed, game resumed. installMbcJar started to persist. The texture binding fix remains valid but was NOT sufficient for the reported leak.

- Latest user rejected texture-state fix as sufficient: visible bloom leak persisted after hot swap and another shaders on->off. Announced screenshot /tmp/ausm-bloom-leak-current.png shows projected oversized source textures. At composite boundary sky pixel(2000,1800), raw bloom436 RGB[1,0,0], blurred440 approx[1,0,0], world2 RGB[.5137,.6745,.9922]. Raw red is already present at renderBloomLayer:109 immediately after native terrain geometry, before tickets/facades. At NothiriumShadowCompileScheduling.drawChunks:182, program115 has cached instanced attribute4 but live glGetAttribLocation=-1; uniform offset cached/live both0. Wrong cached positive attribute routes native bloom to indirect instancing although shader cannot consume offsets. Fix removes numeric-ID-only caching from both chunk offset location lookups, retaining per-batch queries outside section loops. Build/test/hotswap pending at this note. Agent breakpoints removed, game resumed. No visual acceptance yet.

- Texture-unit fix deployment: installMbcJar completed successfully after hot swap; current MBC on-disk AUSM jar includes this fix. Visual confirmation remains user-pending.

- Texture-unit fix validation: nine ShaderlessBloomRoutingTest tests passed; two helper classes hot-swapped at13:12:10/11. Postpatch at renderBloomLayer:161 saved and actual activeTexture both33984 (unit0), saved and actual atlas both323; 40 sections drawn. This corrects the observed unit0=440/unit4=323 mismatch. Agent probes removed, disabled user exceptions preserved, game resumed. Visual acceptance and another shaders on->off cycle still pending; no screenshot/restart/commit/push. installMbcJar started to persist the fix.

- 2026-09-05 latest: user explicitly accepted hand leak fix, then reproduced shaderless bloom leak after shaders on -> off. Live shaderless geometry had program115, FBO7, depth enabled/LEQUAL, perspective projection and viewport3840x2113; 29 sections rendered. At AusmBloomRenderPasses:161 AFTER RenderState.restore, saved activeTexture=33984 and atlas texture0=323, but actual unit0 remained bloom blur texture440 and atlas323 was bound on unit4. Root cause found in AusmBloomProgramLifecycle.bindTextureUniform: raw glActiveTexture followed by cached GlStateManager.bindTexture desynchronizes active-unit cache. Patch synchronizes cache+GL for sampler selection and explicitly restores actual unit/binding as well as cache. Regression test added. Build/hotswap verification pending at this note. No screenshot taken for bloom; warn user before one. User's current prepared scene is x382.88,y75.32,z209.82. No new bloom source exceptions or extraction. Agent probes removed, MBC running.

- Hand leak root cause confirmed and patched 2026-09-05 12:59: During HAND_SOLID ModelRenderer submission (EfficientEntitiesChestCompat47 caller under EntityRenderer hand redirect), indexed blend3 was ENABLED with srcAlpha=ZERO,dstAlpha=ONE. Hand setup only disabled blending; vanilla arm reenabled it without replacing stale factors. Thus normal RGB was updated but background reflection alpha survived. PipelineRuntimeDiagnosticsState7.prepareVanillaHandRenderState now restores SRC_ALPHA/ONE_MINUS_SRC_ALPHA/ONE/ZERO after bindPass using both GlStateManager cache helper and raw GL14 (indexed state can invalidate cached global assumptions). HotSwap confirmed12:59:05/06. Postpatch same live model probe factors[ONE,ZERO], direct GL45 texel read at hand(3000,420) alpha1.0 instead of0.62352943. Eight ShaderlessBloomRoutingTest tests pass including new ordering/alpha regression. No new masking exception. Agent probes removed; game running; visual comparison screenshot announced. No restart/commit/push.

- New prepared night scene captured with approval at /tmp/ausm-hand-leak-scene2.png. IMPORTANT correction to prior cross-frame hypothesis: same-frame33779, x3000,y420, colortex4 read texture704 and RGBA[0,0.40392157,0.35686275,0.003921569] were unchanged at finishHand243, pre-composite376, after COMPOSITE558, and pre-COMPOSITE1530. At COMPOSITE1 draw539, colortex4 uniform14 actually bound704, viewWidth/Height3840/2113. Depthtex1 uniform19 actually bound673 matching expected snapshot; live and opaque depth both0.5562685. Thus stale texture swap/binding is NOT demonstrated; earlier readings from different frames cannot prove overwrite. Actual recent debug sources are config/ausm/debug-shaders/shaders_world0_*.glsl (base shaders_gbuffers_hand file is stale unrelated shader). Compiled world0 composite1 already contains AUSM_HAND_LIGHT_SHAFT_EXCLUSION guarding z0>0.56. Need isolate the sampled effect using actual compiled source, not assume lightshaft or stale colortex4. No rendering code changed; all probes removed, game running.

- 2026-09-05 prepared leaking-hand scene captured with user approval at /tmp/ausm-hand-leak-20260905.png (3906x2226 window image; current framebuffer3840x2113, NOT earlier1375x1103). At hand pixels x2750,y380/410/440, finishHand color uniformly [0.6015625,0.47265625,0.453125,1]. COMPOSITE color also uniform; COMPOSITE1 first introduces middle-pixel stripe (example red0.04248 vs0.05566 either side). At pre-COMPOSITE1, live/depthtex1 agree at all3 pixels [0.55664504,0.5567366,0.55682814]; colortex4 alpha middle0.6156863 vs0.003921569 either side. Pack composite1 uses this as reflection factor. A separate finishHand sample had uniform colortex4 alpha0.003921569. Producer of intervening stale metadata NOT yet proven: GBUFFERS_HAND directives include [COLOR,COMPOSITE,AUX3,AUX1], while COMPOSITE writes/flips only [AUX4,DEPTH], not AUX1. Do not assume an early-hand reorder is sufficient or introduce another masking patch. Samples were across different frames; next useful check is same-frame colortex4/texture-ID tracking from hand through COMPOSITE1. No code fix this investigation. All agent probes removed, user exception breakpoints preserved, MBC debugger/game running.

- Latest follow-up: user says hand model good, residual leaks and darker red facades. Final native bloom mask still interpreted depthtex1 as hand-free and depthtex2 as post-hand after the snapshot change above. Runtime sample [depth1, depth2, live]=[0.5553161,0.9973781,0.5553161] proved reversed comparison. PipelineBloomRendering now compares depthtex2 (pre-hand) against live depth (post-hand). Successfully hot-swapped at 12:32:45; seven ShaderlessBloomRoutingTest tests pass, latest AUSM JAR installed. Visual acceptance pending.
- Facade directional shading: debugger showed shader oldLighting=false, while GlobalFacades diffuseShade always applied LightUtil.diffuseLight unlike ordinary AUSM block rendering. External GlobalFacades source now queries the same shouldDisableDirectionalShading contract via a cached optional reflection hook. Build/remap passed; JAR installed to MBC, backup /tmp/ausm-facade-shading-wVbhj1/GlobalFacades-before.jar. NOT LIVE: JVM rejected the whole two-class batch at 12:34:35 for adding a field. Restart required, not performed. Do not claim red facade visual match is verified; direct external-source quad probes did not bind and caller frames were hidden. Agent probes removed and game resumed. No screenshots, commit or push.

- Latest 2026-09-05 shadered follow-up: user confirmed hand geometry is good after correcting MC_HAND_DEPTH window range from 0..0.125 to 0.4375..0.5625 in PipelineDeferredPassOrchestration and PipelineRuntimeDiagnosticsState7. Light-shaft leak separately traced at hand pixel (1200,140): live depth 0.5553161 but depthtex1 opaque snapshot 0.9983428. Added HandDepthSnapshot depth-only merge of reserved hand pixels into depthtex1 after hand; depthtex2 stays hand-free. Post-reload sample: depth0=depth1=0.5553161, depth2=0.99934566. Visual shaft acceptance still pending. No automatic material bloom extraction reintroduced.
- GlobalFacades had two independent shader-route blockers: its AusmWorldOverlayCompat required deleted recordShaderlessBloomMetadata (runtime context null), and AUSM accepted GBUFFERS_TERRAIN but not the actual GBUFFERS_TERRAIN_CUTOUT pass. Removed obsolete reflection dependency and material-emission bloom qualification in GlobalFacades, accepted the proper cutout pass in PipelineDistantHorizonsInterop. Reload logs now show available=true, route=ausm-terrain, 100 faces/400 vertices; visual emission/bloom acceptance pending.
- All changes above loaded into running MBC via watcher (no restart). New HandDepthSnapshot definition succeeded but initial combined batch rejected because its original class resource was absent; retry touching only the two existing callers succeeded at 12:24:12. Six ShaderlessBloomRoutingTest tests pass; both remapped JARs validated and installed atomically to MBC (AUSM-1.0.4-Java25.jar, GlobalFacades-1.0.3.jar). Previous GlobalFacades archive retained at /tmp/ausm-facade-hotswap-TTc9Fd/GlobalFacades-1.0.3-before.jar. Agent probes removed; debugger running. Shader-on-to-off shaderless bloom displacement remains unresolved; do not conflate it with the hand-depth fix. No commit/push or screenshots.

- CURRENT DIRECTION (supersedes extraction investigations below): user explicitly requested removal of ALL material-driven bloom extraction; texture is the canonical bloom source. Removed terrain replay/early-depth extraction entry points, extraction GLSL/programs, whole-frame brightness fallback, buffer/section extraction metadata and invalidation, and duplicate emissive fallback mesh appends. Deleted `BloomExtractionPlan.java`. Bloom eligibility and framed/GPOM provenance now use texture-resource classification, not light level, shader material ID or a Lumenized name heuristic. Ordinary emissive lighting remains separate under `blockShaderlessMaterialEmission` and `shaderlessMaterialBlockIds`; these cannot qualify geometry for bloom. Native texture-defined BLOOM rendering and composition remain.
- Final texture-only build installed atomically to the sole MBC `AUSM-1.0.4-Java25.jar`; build/install SHA-256 `c32d69b5702e55cc35f7c114f091130e84a178d51ccbd10766144298bbf0ed77`. Compilation/remapping and ZIP integrity passed. Latest full suite again has 97 passed, 1 pre-existing line-limit failure, 2 skipped; all four bloom routing tests pass. This JAR is on disk only, NOT loaded into the running game. Restart required for removed mixin interfaces; do not attempt helper-only reload.
- Structural mixin/interface cleanup requires restart: do NOT hot-swap this removal into the existing JVM. Game has not been stopped/restarted. Targeted `ShaderlessBloomRoutingTest`: 4/4 passed. Full suite: 100 tests, 1 failure, 2 skipped; failure is the pre-existing worktree `ShaderPackManager.java` line limit (1061 > 1000), not touched by this removal. Final packaging/deployment validation recorded in the response. No commit/push. Missing-chunk and visual native-bloom validation remain unresolved until restart and scene checks.
- Latest direction: user reported the early-depth change removed visible Luminous bloom again, and explicitly requested dedicated BLOOM-layer resource configuration instead of another rendering exception. Updated the enabled MBC `BloomTech-1.2.0.zip` only: all 16 Luminous models retain the existing tinted layered parent/base faces but point their overlay at explicit `<color>_bloom.png` assets copied unchanged from QuantumThings' opaque colored textures. Each overlay has colocated CTM metadata (`ctm_version: 1`, `layer: BLOOM`, `extra.light: 15`). Previous overlay references used the mod's translucent `<color>_t` textures with metadata-only additions. ZIP integrity and all 16 metadata/PNG pairs validated. Backup: `BloomTech-1.2.0.zip.pre-dedicated-luminous-20260905.bak`. F3+T resource reload and visual validation are still required. No Java changes made for this pack-only request; previous generic extraction changes remain in the worktree/runtime and must not be called visually accepted.
- Updated 2026-09-05. This file is the project resume handoff; update it in place instead of creating another resume file.
- Current user confirmation: Luminous emission AND shaderless bloom work after the routing fixes below. Remaining reports: bloom ignores depth; some fully loaded chunks do not render. Missing-chunk cause is not established; requested affected view/coordinates, whether it started after the hot swap, and shadered versus shaderless reproduction.
- Latest depth fix (2026-09-05 11:11:28 local): automatic extraction/composition now runs after translucent terrain via `PipelineFrameLifecycle1.renderWorldBlockLayer` -> `PipelineBloomRendering.renderEmissiveBloomExtractionFromWorldPass`, before the hand clears Minecraft depth. The wrapper saves/restores projection/modelview; the later GUI hook skips the already-rendered pass. `NothiriumShadowCompileScheduling` also preserves extraction-owned state instead of disabling depth writes during translucent replay. No block exceptions. Existing portal/external-view guards retained; those paths are not validated by this fix.
- Depth evidence: pre-fix composite center-depth samples included `1.0`; bytecode confirms `clear(256)` before hand rendering; earlier terrain-boundary sample was `0.9917674`. After hot swap, early extraction returned true, scene-depth sample was `0.9931989`, and the late GUI hook logged its already-rendered skip. This verifies timing/state, not visual acceptance of wall occlusion. Four `ShaderlessBloomRoutingTest` tests passed. Missing chunks have NOT been fixed or conclusively attributed to bloom. Agent probes removed; running debugger retained; no screenshot/restart/commit/push.
- Current built/installed JAR SHA-256: `11c3a1d48f64ab849aca1bb8b556e8b26615d9c04fb9722480404e61fdcbd8db`. Live logs confirmed atomic redefinition of `PipelineBloomRendering`, `PipelineFrameLifecycle1`, and `NothiriumShadowCompileScheduling` at 11:11:28; build, install and tests succeeded.
- Latest investigation (2026-09-05, MBC): runtime entry probe proved `disableShaderlessPreGuiHooks=true`, `isPipelineActive=false`, `shaderlessBloomRenderedThisWorldPass=false`. Removed that gate only from `PipelineBloomRendering.renderShaderlessBloomBeforeGui`; optional UI preparation remains unchanged. Extraction then ran with metadata present but returned false/zero draws.
- Second confirmed routing failure: MBC has both Celeritas and Nothirium. `CeleritasCompat.installed()` was true and forced `renderEmissiveExtractionTerrain` into vanilla, which returned zero. AUSM's transformer strips Celeritas render ownership, so installed compile helpers do not identify the mesh backend. Removed only the Celeritas-presence condition from `PipelineChunkUpdateTracking`; retained Nothirium availability and bypass checks.
- Both helper classes were successfully hot-swapped into the current MBC JVM (PID 20552, JDWP 5005, session `MBC Remote Debug`), confirmed by live reload logs at 11:00:27 and 11:05:41 local. Post-fix debugger events at 11:06:02-03 showed `solid=64` and `shaderlessExtractRendered=true`; the existing successful-extraction log appeared at 11:05:42. This proves geometry submission/extraction execution, NOT visible Luminous bloom or acceptance of the earlier displacement fix. User visual confirmation requested; no screenshots taken. Agent probes removed, user disabled exception breakpoints preserved.
- Latest installed/build JAR SHA-256: `ff2cd9cbab8960ef9d993318d66a8116ed39d9ed70a50ff7570e5d83f1880ca6`. `safeMbcHotSwap` plus `installMbcJar` succeeded. Added `ShaderlessBloomRoutingTest` guarding the two routing regressions; both tests passed. No block-specific rendering exception or texture/material change was added in this investigation. No commit/push.
- Current source version is 1.0.4 on Java 25. The last recorded deployment was `AUSM-1.0.4-Java25.jar` in MeatballCraft, with one installed AUSM JAR and matching build/install hashes. Deployment and process state must be checked again before further runtime work.
- Previous debugger evidence: Luminous resolved to pack material `10028`, reached Nothirium fallback geometry with nonzero vertex counts, and reached bloom metadata marking. A sampled composite had scene-depth masking enabled and used the separate bloom-depth texture. These observations do not prove that the Luminous vertex emission payload survives upload and extraction or produces visible bloom.
- The previous investigation found that automatic extraction did not refresh `bloomDepthTexture`, whereas the native Bloom route did. A depth-only replay and snapshot were added to `AusmBloomRenderPasses.renderEmissiveTerrainBloomCount`. Compilation and installation passed, but the change has no recorded visual acceptance. The earlier claim that stale depth conclusively explained both missing Luminous bloom and displacement was stronger than the evidence.
- Historical only: the previous session's hot-swap attempt did not confirm redefinition. Its external attach process was terminated; this did not establish that that JVM's pending operation was cancelled. The current JVM's two successful helper reloads are recorded above. The prior shaders-on -> shaders-off displacement/leak report remains unverified, but the user's latest priority is Luminous emission working with shaderless bloom missing.
- Continue by tracing the actual Luminous path through vertex payload, compiled buffer/section metadata, extraction draw, and composite. The user requested tracing instead of speculative patches and forbids block-specific rendering exceptions to force Luminous bloom. Preserve resource-pack-driven material configuration.
- Previous debugger probes were removed and the client was reported resumed. Recheck live debugger/process state; old PIDs and session state are historical. Warn the user before taking any new screenshot so they can prepare the scene. Preserve unrelated worktree edits; no commit or push was performed for the latest depth patch.
